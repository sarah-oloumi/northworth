# Examples

Only fictional demo data belongs here.

Do not copy real household data into this directory.

## Import Fixtures

- `imports/signed-amount.csv`: one signed amount column where negative values are outflows.
- `imports/split-debit-credit.csv`: separate debit and credit columns.
